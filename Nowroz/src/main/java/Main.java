import ui.CLI;

public class Main {
    public static void main(String[] args) {
        CLI cli = new CLI();  // Create CLI instance
        cli.start();         // Launch the command line interface
    }
}